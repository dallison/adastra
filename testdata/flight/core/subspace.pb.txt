subsystem {
 name: "subspace"

 static_process {
  name: "subspace_server"
  executable: "${runfiles_dir}/subspace/server/subspace_server"
  options {
    notify: false
  }
 }
}

autostart: "subspace"
